# .env
# ---------------------------------------------------------------
# Environment Variables
# These are configuration values that change between environments
# (development vs production). Never commit sensitive .env files to Git.
# ---------------------------------------------------------------

# SQLite database file path
# "file:" prefix tells Prisma it's a file-based SQLite database
DATABASE_URL="file:./database/stone_crusher.db"
